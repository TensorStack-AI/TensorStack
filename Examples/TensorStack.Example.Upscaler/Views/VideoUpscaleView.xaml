<Views:ViewBase x:Class="TensorStack.Example.Views.VideoUpscaleView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
    xmlns:local="clr-namespace:TensorStack.Example.Views"
    xmlns:CommonControls="clr-namespace:TensorStack.WPF.Controls;assembly=TensorStack.WPF"
    xmlns:Views="clr-namespace:TensorStack.Example.Views"
    mc:Ignorable="d" 
    d:DesignHeight="450"
    d:DesignWidth="800">
    <Grid DataContext="{Binding RelativeSource={RelativeSource AncestorType={x:Type local:VideoUpscaleView}}}">
        <Grid.RowDefinitions>
            <RowDefinition Height="1*" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" >
            <Grid.ColumnDefinitions>
                <ColumnDefinition MinWidth="360"/>
                <ColumnDefinition Width="5" />
                <ColumnDefinition Width="4*" />
            </Grid.ColumnDefinitions>


            <!--***Left Panel***-->
            <Grid Grid.Column="0">
                <Border  Margin="4">
                    <DockPanel x:Name="LeftPanelContainer" Margin="4">

                        <StackPanel DockPanel.Dock="Top" IsEnabled="{Binding UpscaleService.IsLoading, Converter={StaticResource InverseBoolConverter}}">

                            <!--Device Selector-->
                            <StackPanel IsEnabled="{Binding UpscaleService.IsLoaded, Converter={StaticResource InverseBoolConverter}}">
                                <TextBlock Text="Device" Style="{StaticResource FieldTextBlockStyle}" />
                                <ComboBox ItemsSource="{Binding Settings.Devices}" SelectedItem="{Binding SelectedDevice}" DisplayMemberPath="Name"/>
                            </StackPanel>

                            <!--Model Selector-->
                            <Grid >
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="3*" />
                                    <ColumnDefinition Width="1*" />
                                </Grid.ColumnDefinitions>
                                <StackPanel Grid.Column="0" IsEnabled="{Binding UpscaleService.IsLoaded, Converter={StaticResource InverseBoolConverter}}">
                                    <TextBlock Text="Models" Style="{StaticResource FieldTextBlockStyle}" />
                                    <ComboBox SelectedItem="{Binding SelectedModel, Mode=TwoWay}"  ItemsSource="{Binding Settings.UpscaleModels}" DisplayMemberPath="Name" />
                                </StackPanel>
                                <Grid Grid.Column="1" Height="22" Margin="0,18,0,0" >
                                    <Button x:Name="ButtonLoad" Content="Load" Command="{Binding LoadCommand}" />
                                    <Button x:Name="ButtonUnload" Content="Unload" Command="{Binding UnloadCommand}" Visibility="{Binding UpscaleService.IsLoaded, Converter={StaticResource BooleanToHiddenConverter}}" />
                                </Grid>
                            </Grid>

                        </StackPanel>

                        <!--Execute/Cancel-->
                        <UniformGrid DockPanel.Dock="Bottom" Columns="2" Height="30">
                            <Button Content="Cancel" Command="{Binding CancelCommand}" />
                            <Button Content="Execute" Command="{Binding ExecuteCommand}" />
                        </UniformGrid>


                        <StackPanel>

                            <!--Input Video-->
                            <Border Margin="0,15,0,15"
                                VerticalAlignment="Top"
                                Style="{StaticResource VideoDropZoneStyle}"
                                Width="{Binding ActualWidth,ElementName=LeftPanelContainer}"
                                Height="{Binding ActualWidth,ElementName=LeftPanelContainer}"
                                MaxHeight="{Binding ActualWidth,ElementName=LeftPanelContainer}">
                                <CommonControls:VideoStreamElement
                                    Source="{Binding SourceVideo, Mode=TwoWay}"
                                    IsLoadEnabled="True"
                                    IsLoadOverlayEnabled="False" 
                                    IsSaveEnabled="False" />
                            </Border>


                            <!--Upscale Options-->
                            <UniformGrid Columns="3">
                                <StackPanel>
                                    <TextBlock Text="Tile Mode" Style="{StaticResource FieldTextBlockStyle}" />
                                    <ComboBox SelectedItem="{Binding TileMode}" ItemsSource="{Binding Source={StaticResource TileMode}}" />
                                </StackPanel>
                                <StackPanel>
                                    <TextBlock Text="Tile Size" Style="{StaticResource FieldTextBlockStyle}" />
                                    <TextBox Text="{Binding TileSize}" />
                                </StackPanel>
                                <StackPanel>
                                    <TextBlock Text="Tile Overlap" Style="{StaticResource FieldTextBlockStyle}" />
                                    <TextBox Text="{Binding TileOverlap}" />
                                </StackPanel>
                            </UniformGrid>

                        </StackPanel>
                    </DockPanel>
                </Border>
            </Grid>


            <!--GridSplitter-->
            <GridSplitter Grid.Column="1" HorizontalAlignment="Stretch" Width="5" Opacity=".4" Margin="0,20" />


            <!--***Middle Panel***-->
            <Grid Grid.Column="2" Grid.Row="0" >
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="7*" />
                    <ColumnDefinition Width="5" />
                    <ColumnDefinition Width="0" />
                </Grid.ColumnDefinitions>


                <!--Content-->
                <Grid Grid.Column="0" >
                    <Border Margin="4">
                        <Grid Margin="20" >
                            <CommonControls:VideoStreamElement 
                                x:Name="ResultControl"
                                SplitterVisibility="Auto"
                                SplitterPosition="Source"
                                SplitterDirection="LeftToRight"
                                Progress="{Binding Progress}"
                                Source="{Binding ResultVideo, Mode=TwoWay}" 
                                OverlaySource="{Binding CompareVideo, Mode=TwoWay}" 
                                IsLoadEnabled="False" 
                                IsLoadOverlayEnabled="False"/>
                        </Grid>
                    </Border>
                </Grid>


                <!--GridSplitter-->
                <GridSplitter Grid.Column="1" HorizontalAlignment="Stretch" Width="5" Opacity=".4" Margin="0,20"/>


                <!--***Right Panel***-->
                <Grid Grid.Column="2" >
                    <Border Margin="4">
                        <!--Debug Panel-->
                        <StackPanel DockPanel.Dock="Bottom" Margin="4">
                            <TextBlock Text="{Binding SelectedDevice.Type, StringFormat={}Device: {0}}" />
                            <TextBlock Text="{Binding SelectedDevice.DeviceId, StringFormat={}DeviceId: {0}}" />
                            <TextBlock Text="{Binding SelectedModel.Name, StringFormat={}Model: {0}}" />
                            <TextBlock Text="{Binding UpscaleService.IsLoaded, StringFormat={}IsLoaded: {0}}" />
                            <TextBlock Text="{Binding UpscaleService.IsLoading, StringFormat={}IsLoading: {0}}" />
                            <TextBlock Text="{Binding UpscaleService.IsExecuting, StringFormat={}IsExecuting: {0}}" />
                            <TextBlock Text="{Binding UpscaleService.CanCancel, StringFormat={}CanCancel: {0}}" />
                        </StackPanel>
                    </Border>
                </Grid>

            </Grid>
        </Grid>

    </Grid>
</Views:ViewBase>
